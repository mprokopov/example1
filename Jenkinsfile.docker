pipeline {
  agent { docker { image 'golang:alpine'} }
  stages {
    stage('Build') {
      steps {
        sh 'go build -o example1'
      }
    }
    stage ('Save artifact') {
      steps {
        archiveArtifacts 'example1'
      }
    }
  }
}
